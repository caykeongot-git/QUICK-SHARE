{
  "name": "quick_share",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix apps/signaling\" \"npm run dev --prefix apps/web\"",
    "build": "npm run build --prefix apps/web",
    "install:all": "npm install && npm install --prefix apps/signaling && npm install --prefix apps/web"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
